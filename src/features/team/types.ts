export type TeamMemberNode = {
  id: string;
  name: string;
  role: string;
  photo: string;
  x: number;
  y: number;
};

export type CanvasTransform = {
  x: number;
  y: number;
  scale: number;
};
